---
title: POST /v2/projects
description: Create a sovseal project (Platform only).
---

import { Callout } from "@/components/callout";

<Callout type="warning" title="Draft">
Endpoint reference incoming. Self-hosted deployments provision projects via the
[deploy guide](/self-hosted/deploy).
</Callout>

## What this page will cover

- TODO: request body schema
- TODO: response shape — returns `project_id` and the first `sov_live_*` key (shown once)
- TODO: post-creation steps (configure SDK, smoke-test replication)
